spring:
  application:
    name: welcome-api
server:
   port : 9001
eureka:
  client:
    service-url:
      defaultZone: http://13.235.107.128:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true